Este exemplo mostra como utilizar o temporizador e o mostrador
de sete segmentos, implementando um contador de segundos.

Para executar, use o montador para gerar o arquivo executável:

lasm -o test.exe test.s

e inicie o simulador:

legsim -l test.exe 

No simulador, execute "g _start".
